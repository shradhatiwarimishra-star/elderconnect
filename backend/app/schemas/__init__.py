"""
Marshmallow Schemas for Serialization/Deserialization
Note: These schemas are optional since we use .to_dict() methods on models.
They're kept here for future use if needed for validation or complex serialization.
"""

# Schemas are not currently used in the application
# We use model.to_dict() methods instead for simplicity
# If you need Marshmallow schemas in the future, you can enable them here
